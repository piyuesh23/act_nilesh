local:
    root: /var/www/exercise/docroot
    uri: 'http://local.exercise.com'
    host: local.exercise.com
    user: vagrant
    ssh: { options: '-o PasswordAuthentication=no -i $HOME/.vagrant.d/insecure_private_key' }
